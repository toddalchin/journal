
import JournalEntries from './JournalEntries';

// This component serves as the entry point for the root URL
// It simply redirects to the JournalEntries page
const Index = () => {
  return <JournalEntries />;
};

export default Index;
